{
  "name": "pontoseven-landing",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Landing do PontoSeven com cadastro e assinatura via Stripe.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node build.mjs",
    "dev": "vercel dev",
    "db:migrate": "node scripts/migrate.mjs",
    "test": "node --test 'tests/**/*.test.mjs'"
  },
  "dependencies": {
    "pg": "^8.23.0",
    "stripe": "^17.5.0"
  }
}
